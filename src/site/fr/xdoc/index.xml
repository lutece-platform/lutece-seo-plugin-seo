<?xml version="1.0" encoding="UTF-8"?>
<document>
    <properties>
        <title>Plugin SEO</title>
    </properties>
    <head>
        <meta name="keywords" content="seo, search, friendly url, canonical url, sitemap, moteur de recherche, réécriture d'URL" />
    </head>
    <body>
        <section name="Plugin SEO">
            <subsection name="Introduction">
                <p>
                    Ce plugin rassemble ce dont un site a besoin pour être correctement indexé par les
                    moteurs de recherche : URL explicites, URL canoniques, sitemap et règles de réécriture
                    d'URL. Le tout se gère depuis une seule fonctionnalité du back office, <i>Gestion
                    SEO</i>, et vit dans deux tables : <code>seo_friendly_url</code> et
                    <code>seo_rule</code>.
                </p>
            </subsection>

            <subsection name="URL explicites">
                <p>
                    Le module des URL explicites vous permet d'exposer une URL lisible pour n'importe
                    quelle ressource de votre site.
                </p>
                <p>Par exemple l'URL technique :</p>
                <p>
                    <code>http://mysite/jsp/site/Portal.jsp?page_id=122</code>
                </p>
                <p>devient accessible sous une forme bien plus conviviale :</p>
                <p>
                    <code>http://mysite/pages/my_topic.html</code>
                </p>
                <p>
                    Elles ne sont pas saisies à la main. Des générateurs parcourent le contenu du site et
                    dérivent une URL par ressource à partir de son nom : <i>Budget 2024</i>, page 122,
                    donne <code>/budget-2024</code>. Trois options en modulent la forme — hériter du
                    chemin des pages parentes, ajouter un suffixe <code>.html</code>, et écraser les URL
                    déjà générées. Tout plugin peut fournir son propre générateur en implémentant
                    <code>FriendlyUrlGenerator</code>.
                </p>
                <p>
                    Les pages continuent de pointer vers l'URL technique tant que le post-processeur de
                    contenu n'est pas activé : il réécrit les liens de chaque page servie, de sorte que
                    visiteurs et moteurs ne voient jamais que les URL explicites.
                </p>
            </subsection>

            <subsection name="URL canoniques">
                <p>
                    Lorsqu'une même ressource est joignable sous plusieurs URL, il faut indiquer aux
                    moteurs laquelle indexer, sans quoi ils dispersent son classement ou en choisissent une
                    d'eux-mêmes. Ce module ajoute aux pages servies un élément
                    <code>&lt;link rel="canonical"&gt;</code> pointant vers l'URL explicite marquée comme
                    canonique.
                </p>
                <p>
                    Le mécanisme s'active depuis le back office, et chaque URL explicite porte son propre
                    marqueur de canonicité.
                </p>
            </subsection>

            <subsection name="Sitemap">
                <p>
                    Un sitemap est la liste des URL d'un site, au format XML défini par
                    <a href="https://www.sitemaps.org">sitemaps.org</a>, destinée aux <b>seuls</b> robots
                    d'indexation — il n'est jamais présenté aux visiteurs, contrairement au plan du site.
                    Il contient toutes les URL explicites marquées comme devant y figurer, avec la date de
                    dernière modification de la ressource correspondante.
                </p>
                <p>
                    Le fichier est généré <b>hors de la webapp</b>, et servi par le plugin lui-même sur
                    <code>/sitemap-seo.xml</code> — le chemin à déclarer dans la directive
                    <code>Sitemap</code> du <code>robots.txt</code> du site. Voir <i>Fichiers générés</i>
                    ci-dessous.
                </p>
                <p>
                    Les localisations qu'il publie doivent être des URL absolues, comme l'exige le
                    protocole et comme le vérifient les moteurs : elles sont construites à partir de
                    <code>lutece.prod.url</code>, une propriété de lutece-core que le site renseigne par
                    environnement. <b>Laissée vide, le sitemap est généré avec des localisations
                    relatives, que les moteurs rejettent</b>, et chaque génération journalise une erreur en
                    ce sens.
                </p>
            </subsection>

            <subsection name="Réécriture d'URL">
                <p>
                    Le module de réécriture d'URL vous permet de créer des règles associant une URL
                    publique à l'URL technique qui la sert. Par exemple :
                </p>
                <p>
                    <code>http://mysite/app/wiki</code>
                </p>
                <p>est servie par</p>
                <p>
                    <code>http://mysite/jsp/site/Portal.jsp?page=wiki</code>
                </p>
                <p>
                    La réécriture a lieu au sein de l'application, sous forme de <i>forward</i> : le
                    navigateur conserve l'URL publique dans sa barre d'adresse, et aucune redirection n'est
                    émise. Deux natures de règles coexistent — les règles génériques de
                    <code>seo_rule</code>, qui emploient des expressions régulières et des groupes de
                    capture pour couvrir d'un coup toute une famille d'URL, et une règle par ligne de
                    <code>seo_friendly_url</code>, ancrée aux deux extrémités.
                </p>
                <p>
                    Le filtre lit les règles dans ces deux tables et les recharge lorsqu'elles changent :
                    une publication depuis le back office atteint donc tous les nœuds d'un cluster. Aucun
                    fichier de configuration n'intervient.
                </p>
            </subsection>

            <subsection name="Page de diagnostic de la réécriture d'URL">
                <p>
                    Le filtre UrlRewriteFilter embarque une page de diagnostic qui affiche les règles de
                    réécriture réellement chargées en mémoire, leurs erreurs d'analyse et les informations
                    de la requête courante. Elle permet de savoir si une règle publiée par le plugin a bien
                    été relue par le filtre, ce que la base seule ne dit pas.
                </p>
                <p>
                    Elle est <b>désactivée par défaut</b>. Pour l'activer sur un environnement de
                    développement, poser <code>seo.urlrewriter.statusEnabled=true</code> dans
                    <code>WEB-INF/conf/plugins/seo.properties</code> ; la page est alors servie sur
                    <code>&lt;context-path&gt;/status</code> (valeur de
                    <code>seo.urlrewriter.statusPath</code>).
                </p>
                <p>
                    Tout paramètre d'initialisation du filtre <code>seo_urlrewriter</code> déclaré dans
                    <code>WEB-INF/plugins/seo.xml</code> peut être surchargé de cette manière, sous le
                    préfixe <code>seo.urlrewriter.</code>. La résolution passant par l'API de configuration
                    du core, la valeur peut tout aussi bien venir d'une propriété système ou d'une variable
                    d'environnement telle que <code>SEO_URLREWRITER_STATUSENABLED</code>.
                </p>
                <p>
                    À ne pas laisser active en production : l'accès est filtré sur l'en-tête
                    <code>Host</code> (<code>seo.urlrewriter.statusEnabledOnHosts</code>), pas sur
                    l'adresse du client.
                </p>
            </subsection>

            <subsection name="Fichiers générés">
                <p>
                    Le sitemap est écrit dans <code>sitemap-seo.xml</code>, au sein du répertoire désigné
                    par la propriété <code>seo.generatedFilesPath</code> de
                    <code>WEB-INF/conf/plugins/seo.properties</code>. Ce répertoire est situé hors de la
                    webapp : une webapp est un artefact de build, remplacé à chaque déploiement, donc un
                    fichier généré qui y réside est perdu — et sur un site qui livre son propre
                    <code>sitemap.xml</code>, écrasé.
                </p>
                <p>
                    Laissée indéfinie, la propriété fait retomber sur un sous-répertoire du répertoire
                    temporaire système. Ce défaut est toujours inscriptible, et sans conséquence puisque le
                    daemon reconstruit le sitemap chaque heure et que la servlet le génère à la demande
                    s'il manque. Pointer la propriété sur un volume persistant conserve le fichier entre
                    les redémarrages.
                </p>
                <p>
                    Aucun conteneur de servlets ne servant un fichier situé hors de la webapp,
                    <code>SitemapServlet</code> le publie, et une règle de réécriture de ce plugin l'expose
                    sur <code>/sitemap-seo.xml</code>.
                </p>
                <p>
                    <b>Migration.</b> Les versions antérieures écrivaient le sitemap dans la webapp, sous
                    le nom <code>sitemap.xml</code> puis <code>sitemap-seo.xml</code>. Supprimer tout
                    fichier de ce genre resté à la racine de la webapp, et faire pointer la directive
                    <code>Sitemap</code> du <code>robots.txt</code> sur <code>/sitemap-seo.xml</code> :
                    tant qu'il en subsiste un, le serveur web continue de servir ce fichier gelé aux
                    moteurs qui l'ont découvert, et chaque génération journalise un rappel. Noter que le
                    <code>robots.txt</code> appartient au dépôt de chaque site, non à ce plugin, et qu'il
                    accepte plusieurs directives <code>Sitemap</code>.
                </p>
                <p>
                    Les règles de réécriture ne sont plus écrites dans un fichier. La propriété
                    <code>seo.configFilePath</code> et le paramètre <code>confPath</code> du filtre ont été
                    supprimés.
                </p>
            </subsection>

            <subsection name="Configuration">
                <table>
                    <tr>
                        <th>Propriété</th>
                        <th>Défaut</th>
                        <th>Rôle</th>
                    </tr>
                    <tr>
                        <td><code>seo.generatedFilesPath</code></td>
                        <td>un sous-répertoire du répertoire temporaire système</td>
                        <td>Répertoire des fichiers générés, hors webapp</td>
                    </tr>
                    <tr>
                        <td><code>daemon.seoFriendlyUrlGenerator.interval</code></td>
                        <td><code>3600</code></td>
                        <td>Secondes entre deux exécutions du générateur d'URL explicites</td>
                    </tr>
                    <tr>
                        <td><code>daemon.seoSitemapGenerator.interval</code></td>
                        <td><code>3600</code></td>
                        <td>Secondes entre deux générations du sitemap</td>
                    </tr>
                    <tr>
                        <td><code>seo.urlrewriter.*</code></td>
                        <td>la valeur déclarée dans <code>seo.xml</code></td>
                        <td>Surcharge un paramètre d'initialisation du filtre de réécriture</td>
                    </tr>
                </table>
                <p>
                    <code>lutece.prod.url</code> est lue également, mais appartient à lutece-core : le site
                    la renseigne par environnement, dans <code>conf/override/config.properties</code>. Ce
                    plugin se contente de la lire.
                </p>
            </subsection>

            <subsection name="Daemons">
                <p>
                    <code>seoFriendlyUrlGenerator</code> régénère les URL explicites à partir du contenu du
                    site, puis publie les règles de réécriture. <code>seoSitemapGenerator</code> régénère
                    le sitemap. Les deux s'exécutent une fois au démarrage puis toutes les heures, et
                    chacun peut être désactivé depuis le back office, indépendamment de l'intervalle.
                </p>
            </subsection>

        </section>
    </body>
</document>
